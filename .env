# === API ===
API_PORT=8080
DATABASE_URL=postgres://verdant:verdant@db:5432/verdant

# === Web (Next.js) ===
NEXT_PUBLIC_API_BASE=http://localhost:8080

# === Postgres (docker-compose) ===
POSTGRES_USER=verdant
POSTGRES_PASSWORD=verdant
POSTGRES_DB=verdant

# Postgres (already present in your setup)
VERDANTOPS_DB_URL=postgres://verdantops:verdantops@db:5432/verdantops

# Carbon defaults (used if no region match)
CARBON_DEFAULT_GRID_INTENSITY_G_PER_KWH=450

# Optional auth (if you want to lock the ingest endpoint)
VERDANTOPS_INGEST_TOKEN=changeme

